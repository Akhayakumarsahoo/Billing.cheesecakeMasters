Implement the menu management . When one clicks on Menu management on the left sidebar it will open the menu management page on the main area.

change - only admin can access this.change this in /context also.

There will be the existing menu for that outlet.
it will show existing categories as a collapsible heading and menu items of that category inside it. Use shadcn for it.
the menu item name and price will be shown inside that collapsible, a three dot button to edit and delete that specific item. add a make sure dialog for deleting that item.

On the top of that page show a button to create new item , another button to create new category.

Make sure no item will be created by not mapping it to a existing category.
at the time of creating a new item it must be mapped to a GST slab also. can be edited later.

at the end make sure every thing works perfect.
